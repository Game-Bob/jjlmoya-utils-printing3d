---
import { SEORenderer } from '@jjlmoya/utils-shared';
import type { SEOSection as SharedSEOSection } from '@jjlmoya/utils-shared';
import { multiMaterialPurgeCalculator } from './index';
import type { KnownLocale } from '../../types';

interface Props {
  locale?: KnownLocale;
}

const { locale = 'en' } = Astro.props;
const content = await multiMaterialPurgeCalculator.i18n[locale]?.();
if (!content) return null;
---

{content.seo?.length > 0 && <SEORenderer content={{ locale, sections: content.seo as SharedSEOSection[] }} />}
