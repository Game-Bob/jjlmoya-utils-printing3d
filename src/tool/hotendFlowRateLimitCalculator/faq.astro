---
import { FAQSection } from '@jjlmoya/utils-shared';
import { hotendFlowRateLimitCalculator } from './index';
import type { KnownLocale } from '../../types';

interface Props {
  locale?: KnownLocale;
}

const { locale = 'en' } = Astro.props;
const content = await hotendFlowRateLimitCalculator.i18n[locale]?.();
---

{content?.faq && <FAQSection items={content.faq} />}
